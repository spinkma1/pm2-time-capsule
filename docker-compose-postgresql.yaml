name: pm2-postgresql
volumes:
  pm2-time-capsule-postgres:

services:
  postgres:
    image: postgres:latest
    ports:
      - '45000:5432'
    volumes:
      - ./database:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: time-capsule
